//we pass this file in as a startup script on the server and when we update from git using our script - our config file gets pulled down.
//after we build a server we image it for easy deployment so this really only gets updated when we roll new server to be imaged
module.exports = config = {

}